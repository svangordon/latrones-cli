// import { fromJS } from 'immutable';
// import { makeSelectPlayPageDomain } from '../selectors';

// const selector = makeSelectPlayPageDomain();

describe('makeSelectPlayPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
